pipeline {
    agent any
    stages {
        stage ('Unit test') {
            steps {
                sh 'docker build -f api/tests/Dockerfile api'
            }
        }
        stage ('Meio') {
            steps {
                sh 'echo meio'
            }
        }
    }
}